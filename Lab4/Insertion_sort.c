#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define MAX 100000

void insertion_sort(int A[], int n)
{
    
}

void display(int A[], int n)
{
    int i;
    for (i = 0; i < n; i++)
    {
        printf("%d ", A[i]);
    }
    printf("\n\n\n");
}

int main()
{
    int i, n, A[MAX];
}